{
  "name": "sistema-rpg-anime-functions",
  "description": "Cloud Functions para o sistema RPG Anime",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "vitest run"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "@google/genai": "^1.47.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true,
  "devDependencies": {
    "vitest": "^4.1.2"
  }
}
